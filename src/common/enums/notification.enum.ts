export enum NotificationChannel {
  EMAIL = 'email',
  WHATSAPP = 'whatsapp',
  SMS = 'sms',
}

export enum NotificationProvider {
  NODEMAILER = 'nodemailer',
  INTERAKT = 'interakt',
  PLIVO = 'plivo',
  TWILIO = 'twilio', // add as needed
}
